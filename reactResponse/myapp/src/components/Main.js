import React from "react";

import BottomAxis from "./BottomAxis";

const Main = (props) => {
  return (
    <div>
      in main
      <BottomAxis></BottomAxis>
    </div>
  );
};

export default Main;
